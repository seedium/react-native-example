import React, { FC } from 'react';

import { Root, TextField } from './message-input.styled';

const MessageInput: FC = () => {
  return (
    <Root>
      <TextField />
    </Root>
  );
};

export { MessageInput };
