import { Model } from 'miragejs';
import type { ModelDefinition } from 'miragejs/-types';
import type { Chat } from '@chat-types/resources/chat.types';

export const ChatModel: ModelDefinition<Chat> = Model;
